let input = ['u', 'D', 'm', 'w', 'b', 'a', 'y', 's', 'i', 's', 'w', 'a', 'e', 's', 'e', 'o', 'm', ' ', ' ']

function sortArray(arr) {
    const key = 'Dumbways is awesome'
    let newArray = []
    for (let i = 0; i < arr.length; i++) {
        console.log(arr[i])
    }
}

sortArray(input)